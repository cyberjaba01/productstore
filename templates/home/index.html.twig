{% extends 'base.html.twig' %}

{% block body %}

<h1>Welcome to homepage, {% if app.user %} {{ app.user.userIdentifier }} {% else %} guest {% endif %}</h1>

<span>/</span>
<a href={{ path('product_index') }}>Products</a>

{% if app.user %}
<a href={{ path('app_logout') }}>Log out</a>
{% else %}
<a href={{ path('register_index') }}>Sign in</a>
<a href={{ path('app_login') }}>Log in</a>
{% endif %}

{% endblock %}